<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">

        <!-- Xposed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Refactored wechat2telecom as LSPosed hybrid module." />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- IPC Bridge: Custom Service for Binder communication -->
        <service
            android:name=".ipc.TelecomBridgeService"
            android:enabled="true"
            android:exported="true"
            android:permission="com.example.wechat2telecom.PERMISSION_WECHAT_BRIDGE">
            <intent-filter>
                <action android:name="com.example.wechat2telecom.ACTION_BIND_BRIDGE" />
            </intent-filter>
        </service>

    </application>

    <!-- Custom permission for IPC Bridge security -->
    <permission
        android:name="com.example.wechat2telecom.PERMISSION_WECHAT_BRIDGE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.example.wechat2telecom.PERMISSION_WECHAT_BRIDGE" />

</manifest>